# environment.yml
name: scoundrel
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
    - rich>=13.0.0
    - pytest>=7.0.0
    - black>=23.0.0
    - isort>=5.0.0
    - torch>=2.9.1
    - numpy>=2.2.6
    - tensorboard>=2.20.0
    - scipy>=1.14.0
    - matplotlib>=3.10.0